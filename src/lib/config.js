export const APP_CONFIG = {
    // Enable switching between multiple families (history list)
    // Set to false to restrict users to a single family view
    ENABLE_FAMILY_SWITCHING: false,

    // Max number of families to keep in history
    MAX_FAMILY_HISTORY: 5
};
